name: 🚀 Deploy CLIENT to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: connect to server
        run: |
          ssh admin@192.250.238.176 -p 1969
          ${{secrets.GITHUB_PASSWORD}}

      - name: update client
        run: |
          /scripts/update_jorycia.sh
